
```cardlink
url: https://hyperskill.org/learn/step/4625
title: "Hyperskill"
host: hyperskill.org
favicon: https://hs.azureedge.net/static/hyperskill.org/favicons/favicon.ico
image: https://hyperskill.org/static/hyperskill-hypercover.png
```



이 주제에서는 Kotlin의 [조건식을](https://hyperskill.org/learn/step/4625 "Kotlin에서 조건식은 프로그램이 부울 표현식의 값에 따라 다양한 계산을 수행할 수 있도록 하는 구문입니다. | 표현식 스타일 구문으로, 계산 결과로 값을 반환할 수 있으며, 그 결과는 본문의 마지막 표현식이 됩니다. 조건식에는 단일 if, if-else, if-else-if, 중첩 if 등 다양한 형태가 있습니다. 다른 프로그래밍 언어와 달리 Kotlin의 if 표현식은 명령문이 아니라 표현식입니다. 이 항목에서는 Kotlin의 조건식을 살펴보고, 다양한 형태의 표현식과 표현식 스타일 구문으로서의 사용법을 중점적으로 살펴보겠습니다.") 살펴보고 , 다양한 형태의 `if`표현식과 표현식 스타일 구성으로서의 사용법에 초점을 맞춥니다.

## 조건식

[조건식을 사용하면 프로그램이 부울 표현식](https://hyperskill.org/learn/step/4625 "코틀린에서 부울 표현식은 참 또는 거짓의 부울 값으로 평가되는 표현식 유형입니다. | 이러한 표현식은 프로그래밍에서 두 가지 부울 조건에 따라 결정을 내리는 데 자주 사용됩니다. 'not', 'and', 'or', 'xor'와 같은 논리 연산자를 포함할 수 있으며, 이를 통해 복잡한 값 매칭을 지원하고 코드 이해를 도울 수 있습니다. 코틀린에서 부울 값은 매장의 개점 여부와 같이 두 가지 상반된 상태를 나타내는 데 자주 사용되며, 참 또는 거짓의 두 가지 값만 가질 수 있습니다.") 의 값에 따라 다양한 계산을 수행할 수 있습니다 . 조건식에는 **단일 if** , **if-else** , **if-else-if** , 그리고 중첩된 **if** 등 다양한 형태가 있습니다 .

## 표현형 "if"

다른 프로그래밍 언어와 달리 Kotlin은 `if`문장이 아니라 표현식입니다 [.](https://hyperskill.org/learn/step/4625 "Kotlin에서 명령문은 텍스트를 인쇄하거나 변수에 값을 할당하는 것과 같이 실행할 단일 명령입니다. | 계산 결과로 단일 값을 생성하는 표현식과 달리 명령문은 프로그램 상태를 변경하는 것과 같은 부작용이 있는 독립형 명령입니다. 명령문의 결과는 프로그램에서 사용되지 않지만 표현식의 결과는 종종 사용됩니다. 예를 들어, `val x = 2 * 2` 명령문에서 `2 * 2` 표현식이 평가되고 그 결과가 `x` 변수에 할당됩니다. 그러나 명령문 자체의 결과는 프로그램에서 사용되지 않습니다. 반대로 `println(2 * 2)`라고 작성하면 `2 * 2` 표현식이 평가되고 그 결과가 `println` 함수에 직접 전달되어 결과가 인쇄됩니다. 명령문은 표현식이 될 수 있지만 모든 표현식이 명령문인 것은 아니라는 점에 유의하는 것이 중요합니다. 예를 들어, `2 * 2`는 표현식이지만 프로그램의 상태를 변경하지 않으므로 명령문이 아닙니다.") 즉, 계산 결과로 값을 반환할 수 있습니다. 결과는 본문의 마지막 표현식이어야 합니다. 예를 들면 다음과 같습니다.

```kotlin
val max = if (a > b) {
    println("Choose a")
    a
} else {
    println("Choose b")
    b
}
```

위 예에서 변수에는 `max`본문의 마지막 표현식 값이 할당됩니다. 표현식 스타일을 사용하는 경우 분기가 `if`있어야 한다는 점에 유의해야 합니다 `else`.

모든 본문에 단일 문장만 포함되어 있는 경우 중괄호를 건너뛸 수 있습니다.

```kotlin
val max = if (a > b) a else b
```

결과를 저장하기 위해 새 변수를 선언할 필요가 없는 경우도 있습니다. 예를 들어 다음 예를 살펴보겠습니다.

```kotlin
fun main() {
    val a = readln().toInt()
    val b = readln().toInt()

    println(if (a == b) {
        "a equal b"
    } else if (a > b) {
        "a is greater than b"
    } else {
        "a is less than b"
    })
}
```

위의 예에서 표현식은 변수를 선언하지 않고 함수 `if`에 직접 전달되고 결과를 인쇄합니다.`println()``println()`

## 관용구

결과를 얻어야 할 때는 표현식 스타일을 사용하는 것이 좋습니다 `if`. 변경 가능한 변수나 잊어버린 변경 사항으로 인해 발생할 수 있는 잠재적인 문제를 방지하는 데 도움이 될 수 있습니다. 예:

```kotlin
val max = if (a > b) a else b // one line
```

요약하자면, Kotlin의 `if`표현식은 코드의 조건부 실행을 처리하는 강력하고 유연한 방법을 제공합니다. 표현식 스타일을 사용하면 `if`값을 반환하고 코드를 간소화하는 기능을 활용할 수 있습니다. Java에 익숙하다면 이를 삼항 연산자에 매핑할 수 있습니다.

```java
final String msg = num > 10 
  ? "Number is greater than 10" 
  : "Number is less than or equal to 10";
```

## "if-else-if" 체인보다 더 강력한 대안으로 "when" 사용

**코틀린은 if-else-if** 체인 보다 더욱 강력하고 표현력이 풍부한 대안 인 `when`표현식(expression)을 제공합니다. `when`표현식은 여러 조건을 처리하는 과정을 간소화하고 코드의 가독성을 높여줍니다.

다음은 사용 방법의 예입니다 `when`.

```kotlin
val number = 5

when (number) {
    1 -> println("One")
    2 -> println("Two")
    3 -> println("Three")
    4 -> println("Four")
    else -> println("Number is greater than four")
}
```

이 예제에서 `when`표현식은 변수 값을 `number`여러 조건에 따라 확인합니다. 값이 조건 중 하나와 일치하면 해당 코드 블록이 실행됩니다.

## "언제"를 표현으로 사용하기

표현식 과 유사하게 `if`, `when`값을 반환하는 표현식으로도 사용할 수 있습니다. 표현식이 반환하는 값은 `when`일치하는 분기의 마지막 표현식의 결과입니다.

```kotlin
val number = 3
val message = when (number) {
    1 -> "One"
    2 -> "Two"
    3 -> "Three"
    4 -> "Four"
    else -> "Number is greater than four"
}

println(message) // Output: Three
```

## 범위 및 조건에 "when" 사용

`when` 표현식은 범위 및 더 복잡한 조건에도 사용할 수 있습니다. 예를 들면 다음과 같습니다.

```kotlin
val number = 15

when {
    number < 0 -> println("Negative number")
    number in 1..10 -> println("Number between 1 and 10")
    number % 2 == 0 -> println("Even number")
    else -> println("Odd number greater than 10")
}
```

이 예에서 `when`표현식은 범위와 사용자 정의 조건을 포함한 다양한 조건에 대해 값을 확인합니다 `number`.

## 결론

결론적으로, 코틀린은 코드의 조건부 실행을 처리하는 표현식 `if`과 같은 강력한 구문을 제공합니다 `when`. 이러한 표현식은 값을 반환할 수 있어 코드를 더욱 간결하고 표현력 있게 만듭니다. 이러한 구문을 효과적으로 사용하면 코틀린에서 가독성과 유지 관리가 용이한 코드를 작성하는 데 도움이 됩니다.


```cardlink
url: https://hyperskill.org/learn/step/12553
title: "Hyperskill"
host: hyperskill.org
favicon: https://hs.azureedge.net/static/hyperskill.org/favicons/favicon.ico
image: https://hyperskill.org/static/hyperskill-hypercover.png
```


아시다시피 Kotlin에는 , , 와 같은 다양한 [데이터 타입이](https://hyperskill.org/learn/step/12553 "Kotlin에서 데이터 유형은 변수, 표현식 또는 함수 매개변수가 보유할 수 있는 값의 종류를 지정하는 분류입니다. | Kotlin과 같은 정적 타입 언어의 필수적인 측면으로, 변수와 함수 매개변수에 대한 명시적 또는 암시적 타입 선언이 필요합니다. 예를 들어 Kotlin에는 정수, 부동 소수점 숫자, 문자, 부울, 문자열과 같은 다양한 데이터 유형이 있습니다. 이 중 문자열은 큰따옴표로 묶인 0개 이상의 문자 시퀀스를 나타내는 널리 사용되는 데이터 유형입니다. 문자열은 조건문과 정규 표현식에서 자주 사용되며 길이를 얻거나 두 문자열을 연결하는 등 다양한 연산을 수행합니다. 또한, 타입 추론은 Kotlin의 유용한 기능으로, 컴파일러가 변수, 표현식 또는 함수 매개변수의 데이터 유형을 자동으로 추론하여 코드를 더 간결하고 읽기 쉽게 만듭니다.") 있습니다 . 이 글에서는 이 타입에 대해 집중적으로 살펴보겠습니다 . 간단한 "Hello world!" 프로그램에서도 문자열을 사용하므로 이 타입은 매우 유용합니다. 문자열은 "John", ""처럼 [큰따옴표](https://hyperskill.org/learn/step/12553 "코틀린에서 큰따옴표는 일련의 문자를 나타내는 문자열 유형을 정의하는 데 사용되는 기호입니다. | 코틀린에서 가장 널리 사용되는 데이터 유형 중 하나이며, 큰따옴표로 묶인 문자 수에 따라 결정됩니다. 문자열 연결은 문자열에서 흔히 사용되는 연산으로, 다른 문자열들을 사용하여 하나의 문자열을 구성하는 데 사용됩니다. 두 문자열을 연결하면 새로운 문자열이 생성됩니다.") 로 묶인 0개 이상의 문자로 이루어진 문자열입니다 . 앞으로의 프로젝트에서도 문자열을 자주 사용하게 될 것입니다. 이 글에서는 문자열 작업의 중요한 기본 사항을 살펴보겠습니다.`String``Int``Double``String`[](https://hyperskill.org/learn/step/12553 "코틀린에서 큰따옴표는 일련의 문자를 나타내는 문자열 유형을 정의하는 데 사용되는 기호입니다. | 코틀린에서 가장 널리 사용되는 데이터 유형 중 하나이며, 큰따옴표로 묶인 문자 수에 따라 결정됩니다. 문자열 연결은 문자열에서 흔히 사용되는 연산으로, 다른 문자열들을 사용하여 하나의 문자열을 구성하는 데 사용됩니다. 두 문자열을 연결하면 새로운 문자열이 생성됩니다.")

## 문자열의 길이

종종 .의 길이를 구해야 합니다 `String`. 문자열의 길이는 큰따옴표로 묶인 문자 수에 따라 결정됩니다. 이를 위해 `.length`which를 사용하면 다음 유형의 값을 얻을 수 있습니다 `Int`.

```kotlin
val language = "Kotlin"
println(language.length) // 6

val empty = ""
println(empty.length) // 0
```

## 문자열 연결

또 다른 일반적인 연산은 **[연결](https://hyperskill.org/learn/step/12553 "Kotlin에서 연결은 두 개 이상의 문자열을 하나의 문자열로 결합하는 프로세스를 의미합니다. | 이는 문자열을 다룰 때 흔히 사용되는 연산이며, 작은 문자열에서 큰 문자열을 만드는 데 자주 사용됩니다. Kotlin의 연결 연산자는 `+` 기호로, 두 문자열을 결합하는 데 사용할 수 있습니다. 문자열을 연결할 때는 문자열의 순서가 중요하며, 결과 문자열은 배열된 순서대로 생성됩니다. 연결은 가환 연산이 아니므로 덧셈과 다릅니다. `+` 연산자는 문자열에 다른 유형의 값을 추가하는 데에도 사용할 수 있습니다. 이 경우 값은 자동으로 문자열로 변환된 다음 대상에 연결됩니다. 표현식은 문자열로 시작해야 하며, 다른 유형으로 시작하면 `toString()` 함수를 사용하여 문자열로 변환됩니다. Kotlin에서 문자열 객체를 생성하는 두 가지 주요 방법이 있습니다. 생성자를 사용하거나 `StringBuffer` 클래스와 `append()` 메서드를 사용하는 것입니다. `+` 연산자를 사용하면 결합된 문자열을 저장하기 위한 새로운 메모리가 할당됩니다. 그러나 `StringBuffer` 클래스와 `append()` 메서드를 사용하면 새로운 메모리가 할당되지 않습니다.")**`String` 입니다 . 이는 다른 문자열들을 사용하여 하나의 문자열을 구성하는 데 사용됩니다.**[](https://hyperskill.org/learn/step/12553 "Kotlin에서 연결은 두 개 이상의 문자열을 하나의 문자열로 결합하는 프로세스를 의미합니다. | 이는 문자열을 다룰 때 흔히 사용되는 연산이며, 작은 문자열에서 큰 문자열을 만드는 데 자주 사용됩니다. Kotlin의 연결 연산자는 `+` 기호로, 두 문자열을 결합하는 데 사용할 수 있습니다. 문자열을 연결할 때는 문자열의 순서가 중요하며, 결과 문자열은 배열된 순서대로 생성됩니다. 연결은 가환 연산이 아니므로 덧셈과 다릅니다. `+` 연산자는 문자열에 다른 유형의 값을 추가하는 데에도 사용할 수 있습니다. 이 경우 값은 자동으로 문자열로 변환된 다음 대상에 연결됩니다. 표현식은 문자열로 시작해야 하며, 다른 유형으로 시작하면 `toString()` 함수를 사용하여 문자열로 변환됩니다. Kotlin에서 문자열 객체를 생성하는 두 가지 주요 방법이 있습니다. 생성자를 사용하거나 `StringBuffer` 클래스와 `append()` 메서드를 사용하는 것입니다. `+` 연산자를 사용하면 결합된 문자열을 저장하기 위한 새로운 메모리가 할당됩니다. 그러나 `StringBuffer` 클래스와 `append()` 메서드를 사용하면 새로운 메모리가 할당되지 않습니다.")**

두 개의 문자열은 다음을 사용하여 연결될 수 있습니다 `+`.

```kotlin
val str1 = "ab"
val str2 = "cde"
val result = str1 + str2 // "abcde"
```

두 개의 문자열을 연결하면 새로운 문자열이 생성됩니다.

```kotlin
val one = "1"
val two = "2"
val twelve = one + two 
println(one)      // 1, no changes
println(two)      // 2, no changes
println(twelve)   // 12
```

동일한 표현식에서 여러 문자열을 연결할 수 있습니다.

```kotlin
val firstName = "John"
val lastName = "Smith"
val fullName = firstName + " " + lastName
```

주의하세요 **,** 는 덧셈과 달리 결합 연산은 교환 연산이 아니기 때문에 `str1 + str2`같지 않습니다 .`str2 + str1`

## 문자열에 값 추가

는 a에 다른 유형의 값을 [추가하는](https://hyperskill.org/learn/step/12553 "In Kotlin, the term append refers to the process of adding elements or values to a string. | The `append` function can be used to add values of different types to a string, which are then automatically converted to strings and concatenated to the target. It's important to note that an expression must start with a string for appending to work. Appending can be useful in various scenarios, such as string concatenation, logging and formatting, generating HTML or XML markup, concurrent data processing, multi-threaded text generation, and thread-safe string manipulation.")`+` 데에도 사용할 수 있습니다 . 값은 자동으로 a로 변환된 후 대상에 연결됩니다 .[](https://hyperskill.org/learn/step/12553 "코틀린에서 'append'라는 용어는 문자열에 요소나 값을 추가하는 과정을 의미합니다. | `append` 함수를 사용하면 문자열에 다양한 유형의 값을 추가할 수 있으며, 추가된 값은 자동으로 문자열로 변환되어 대상에 연결됩니다. 'append' 기능이 작동하려면 표현식이 문자열로 시작해야 한다는 점에 유의해야 합니다. 'append' 기능은 문자열 연결, 로깅 및 서식 지정, HTML 또는 XML 마크업 생성, 동시 데이터 처리, 다중 스레드 텍스트 생성, 스레드 안전 문자열 조작 등 다양한 상황에서 유용하게 사용할 수 있습니다.")`String``String``String`

```kotlin
val stringPlusBoolean = "abc" + 10 + true
println(stringPlusBoolean) // abc10true

val code = "123" + 456 + "789"
println(code) // 123456789
```

표현식은 .으로 시작해야 합니다 `String`.

아래 예를 살펴보세요. 첫 번째 피연산자가 숫자이기 때문에 작동하지 않습니다.

```kotlin
val errorString = 10 + "abc" // an error here!
```

다른 상황을 생각해 보겠습니다.

```kotlin
val stringAndNumbers = "abc" + 11 + 22
println(stringAndNumbers) // abc1122
```

왜 이렇게 작동했을까요? 먼저 `11`문자열에 추가하고 `"abc"`, 그다음 `22`문자열에 추가합니다 `"abc11"`.

문자를 a와 연결하면 `String`새로운 문자를 얻을 수 있습니다 `String`.

```kotlin
val charPlusString = 'a' + "bc"
println(charPlusString) // abc
val stringPlusChar = "de" + 'f'
println(stringPlusChar) // def
```

또한, 문자와 문자열을 더하면 다음과 같은 결과가 나오므로 결과에 원하는 값을 추가할 수 있습니다 `String`.

```kotlin
val charPlusStringPlusInt = 'a' + "bc" + 123
println(charPlusStringPlusInt) // abc123
```

문자 처리 방법은 나중에 설명하겠지만, 지금은 문자와 문자열을 연결하여 `String`값을 얻을 수 있다는 점만 기억해 두세요. 문자와 정수의 관계에 관심이 있다면 이 [주제를](https://hyperskill.org/learn/step/4680) 참조하세요 .

## 문자열 반복

한 문자열을 두 번 이상 반복해야 하는 경우 루프를 유지합니다. Kotlin에서는 다음 `repeat`함수를 제공합니다.

```kotlin
print("Hello".repeat(4)) // HelloHelloHelloHello
```

이제 당신의 친구인 선임 개발자가 당신에게 최고의 프로그래머가 되는 비법을 알려줬다고 상상해보세요.

![해야 할 일: 식사, 수면, 코딩, 반복](https://ucarecdn.com/a9643a6b-016f-4d99-8892-0c3d2bfe10f2/)

이 종이를 일주일 내내 일정을 출력하는 코드 조각으로 바꿔보겠습니다.

```kotlin
println("Eat. Sleep. Code.\n".repeat(7)) // \n gives you a line feed (new line)
```

다음은 주간 일정입니다.

```no-highlight
Eat. Sleep. Code.
Eat. Sleep. Code.
Eat. Sleep. Code.
Eat. Sleep. Code.
Eat. Sleep. Code.
Eat. Sleep. Code.
Eat. Sleep. Code.
```

미루는 시간은 없습니다!

## 생줄

**문자열에 탭이나 따옴표 같은 특수 기호가 필요할 때가 있습니다. 이스케이프 시퀀스를** 사용하면 됩니다 . 예를 들면 다음과 같습니다.

```kotlin
// prints 'H' is the first letter of "Hello world!" string.
println("\'H\' is the first letter of \"Hello world!\" string.")
```

좀 무거워 보이네요. 줄바꿈이나 특수문자가 포함된 꽤 긴 텍스트를 작성해야 한다면 읽기 어려울 수 있습니다.

**[이러한 경우에는 원시 문자열을](https://hyperskill.org/learn/step/12553 "Kotlin에서 원시 문자열은 이스케이프 시퀀스를 특수 문자로 취급하지 않는 문자열 리터럴입니다. | 즉, 백슬래시(\)와 기타 특수 문자는 줄 바꿈이나 따옴표 등을 나타내는 데 사용되는 것이 아니라 문자열 내에서 리터럴 문자로 취급됩니다. 원시 문자열은 삼중 따옴표()로 묶이며 여러 줄에 걸쳐 나타날 수 있습니다. 원시 문자열은 이스케이프 처리 없이 리터럴 백슬래시나 기타 특수 문자를 문자열에 포함하려는 경우에 유용합니다.")** 사용할 수 있습니다 . 원시 문자열에는 줄바꿈 문자와 기타 모든 문자가 포함될 수 있습니다. 텍스트를 삼중 따옴표( `"""`) 로 감싸기만 하면 됩니다 .

```kotlin
val largeString = """
    This is the house that Jack built.
      
    This is the malt that lay in the house that Jack built.
       
    This is the rat that ate the malt
    That lay in the house that Jack built.
       
    This is the cat
    That killed the rat that ate the malt
    That lay in the house that Jack built.
""".trimIndent() // removes the first and the last lines and trims indents
print(largeString)
```

이 텍스트는 다음을 인쇄합니다.

```no-highlight
This is the house that Jack built.

This is the malt that lay in the house that Jack built.

This is the rat that ate the malt
That lay in the house that Jack built.

This is the cat
That killed the rat that ate the malt
That lay in the house that Jack built.
```

보시다시피, . 함수도 사용했습니다 `.trimIndent()`. 이 함수는 공통 최소 들여쓰기의 모든 줄을 잘라내고, 첫 번째 줄과 마지막 줄이 비어 있으면 제거합니다. 예를 들면 다음과 같습니다.

```kotlin
val unevenString = """
        123
         456
          789""".trimIndent()
print(unevenString)
println()

val rawString = """123
         456
          789
""".trimIndent()
print(rawString )
```

이 코드는 다음을 인쇄합니다.

```no-highlight
123
 456
  789

123
         456
          789
```

## 결론

이제 가장 기본적인 `String`연산을 알게 되셨네요! 이 함수들은 꽤 간단하지만 프로그래밍에 있어서는 정말 중요합니다. 사실 다른 유용한 연산들도 많지만 `String`, 나중에 자세히 알아보겠습니다. 좋은 작업 되시길 바랍니다!

